import { ref, reactive } from 'vue'
import "@/styles/login/login.scss";

export default{
    setup(){
        return ()=>{
            return (
                <div>
                    
                </div>
            )
        }
    }
}